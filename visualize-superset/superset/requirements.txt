# Superset and dependencies
apache-superset==4.0.0

# Database drivers
psycopg2-binary==2.9.9

# Note: SQLAlchemy, Redis, Celery versions are managed by apache-superset
# Do not specify versions that conflict with superset's requirements

# Additional requirements for custom features
requests==2.31.0
python-dotenv==1.0.0
